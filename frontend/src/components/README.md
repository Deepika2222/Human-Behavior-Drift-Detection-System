/**
 * Components Directory
 * 
 * This directory contains reusable React components for the Human Behavior Drift Detection System.
 * Components should be modular, reusable, and follow React best practices.
 * 
 * Expected components:
 * - BehaviorChart: Visualizes behavior patterns and trends
 * - DriftAlert: Displays drift detection alerts
 * - DataUpload: Handles behavior data file uploads
 * - RiskIndicator: Shows risk scores and severity levels
 * - ModelMetrics: Displays model performance metrics
 * - NavigationBar: Main navigation component
 * - Footer: Application footer
 */
